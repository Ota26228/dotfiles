# AGS generated files (synapse-version is runtime state)
ags/.synapse-version

# Alacritty backup files
alacritty/*.bak

# nvim lazy lockfile (optional: remove if you want to pin versions)
nvim/lazy-lock.json
